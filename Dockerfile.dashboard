FROM python:3.11-slim AS base
WORKDIR /app
COPY pyproject.toml .
RUN pip install --no-cache-dir -e .

FROM base AS production
COPY kpi_lens/ ./kpi_lens/
CMD ["streamlit", "run", "kpi_lens/dashboard/app.py", "--server.port", "8501", "--server.address", "0.0.0.0"]
