FROM python:3.11-slim AS base
WORKDIR /app
COPY pyproject.toml .
RUN pip install --no-cache-dir -e .

FROM base AS production
COPY kpi_lens/ ./kpi_lens/
COPY config/ ./config/
CMD ["uvicorn", "kpi_lens.api.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "2"]
